import {inspired, selfDirected, selfPortrait, box, lastPiece, collage1, collage2} from "../../pictures/pictures.js"

export default function Artworks() {
    <div>
        <h1>something</h1>
        <section>
            <h3>{inspired}</h3>
        </section>
    </div>
}